change the method of http://localhost:8081/auth/changeRole get->put
apply security filter in gateway auth

18-08-2026
working on studentEntity
we have to cache the users that prevents every time fetching from database
